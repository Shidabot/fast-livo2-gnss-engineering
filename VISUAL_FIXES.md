# 视觉模块修复说明

此次修改基于前一次 RTK 修复版本。未修改 IMU 处理文件；已发现的 IMU 起始传播时间等问题仍需修复。

## 实际改动

- 迭代中每次重投影检查正深度、有限值和金字塔实际尺度下的完整采样范围。
- 当前图像、参考图像采样使用实际行跨度；图像入口保证其余旧函数使用的连续灰度存储条件。
- 两条状态更新路径返回成功状态；每次迭代清零残差和雅可比，避免被跳过的点留下旧观测。
- 检查有效残差数量和迭代内观测集合一致性；无法求解、异常状态、误差上升或非法协方差时回退整个视觉更新。
- 最后一次状态增量必须经过一次残差评估；协方差只在整次更新成功后提交，并检查有限性、对称性和正定性。
- 采用 LDLT 分解计算原有滤波更新中的先验信息和解矩阵，替代显式 inverse() 调用。
- 加入 Huber 像素残差权重；权重同时作用于残差和雅可比。拒绝低纹理参考小块。
- 深度图保留最近的正深度，避免遮挡判断随点遍历顺序改变。
- 拒绝空图像、错误类型和不符合标定分辨率的图像；转换异常不会遗留锁。
- 检查逆曝光参数范围，曝光估计时收敛条件包含曝光增量。
- 完善参考图像空指针、配对数量和 NCC 计算：初始化缓存均值，每对观测重置累加器，避免除零。
- 地图为空时重置旧视觉子图和点数；不足最小观测数时仍允许建立地图，避免初始化被少量特征卡住。
- 小块缓存改用常量引用，减少循环中的复制。
- 修复网格数量计算中的整数除法截断，使右侧和底部的部分网格也有对应存储；检查无效网格配置。

逆向组合路径的原六列缓存雅可比未建模曝光，且假设搜索层为零。
当曝光估计开启或搜索层非零时，自动使用正向路径，避免套用不一致的缓存；其余情况保留逆向路径。

## 配置参数

参数位于 `vio` 下，均有默认值，原配置可直接加载：

| 参数 | 默认值 | 含义 |
| --- | --- | --- |
| huber_delta | 20.0 | 曝光缩放后的亮度残差转入线性惩罚的阈值 |
| min_visual_points | 3 | 更新所需最小完整小块数；仅是安全下限，不保证可观测性 |
| min_patch_variance | 1.0 | 参考小块最小亮度方差 |
| min_inv_exposure | 0.05 | 逆曝光参数下限 |
| max_inv_exposure | 20.0 | 逆曝光参数上限 |
| ncc_en | false | 是否启用原有 NCC 初筛 |
| ncc_thre | 0.6 | NCC 最小相关系数 |

阈值需要根据实际图像质量调参。当前失败回退策略较保守；动态物体语义识别和观测空间分布判据未新增。
没有改动相机时间偏移或外参，二者必须依据设备标定结果设置。

## 验证范围

当前环境没有可用 C++ 编译器、ROS 和 GTSAM，未完成整项目编译、C++ 测试执行和数据回放。
已提供独立边界测试，覆盖金字塔采样范围、Huber 权重/损失与深度遮挡：

```bash
g++ -std=c++17 -Wall -Wextra -pedantic -Iinclude tests/visual_utils_test.cpp -o /tmp/visual_utils_test
/tmp/visual_utils_test
```

源码差异和括号完整性、配置、ZIP 完整性及补丁可应用性在交付前检查。
回放必须覆盖：正常图像、空图像、错误分辨率、边缘投影、近零深度、弱纹理、曝光变化及前景遮挡。
分别比较正常/逆向组合配置，验证拒绝更新后状态和协方差不变，视觉地图可以从少量特征继续建立。
新增测试未覆盖整个滤波器；精度和速度收益仍需实测，静态检查不能替代运行验证。
