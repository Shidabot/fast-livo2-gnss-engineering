# RTK 修复说明

本次修复保持 FAST-LIVO2 实时建图、RTK 离线批量优化的架构。
`gps/gps_en` 控制 GNSS 后端和前端 GNSS 显示，不启用实验性的在线融合。
在线 RTK 的杆臂、观测噪声、初始化可观测性仍需独立设计和实测。

## 修复内容

- GNSS 回调与关键帧使用同一个互斥锁；优化前冻结两种输入，防止时间对齐过程中队列被并发修改。
- 空数据、样条构建失败、匹配不足、静止或共线轨迹会明确退出，避免继续访问不完整数组。
- 检查配置中的外参数量、非有限值、正方差和时间偏移。
- 检查 GNSS valid_fix、三维定位类型、经纬度、时间和精度；后端同时检查速度，丢弃重复和倒序时间。
- 修复精度距离直接当方差的问题；GNSS 因子使用逐条观测精度平方，并保留配置中的方差下限。
- GNSS 因子仅用于时间范围内且采样间隔不超过 opt/max_rtk_gap 的关键帧；缺测范围不再被外推为约束。
- 短时间重叠或录制时长不同不再触发粗时间基准平移；对齐窗口从两种数据共同覆盖的起点开始。
- 优化参数实际传给求解器；使用 COLAMD 排序，避免引入额外 METIS 要求；捕获后台异常。
- 优化前后地图、LIVO 轨迹统一输出 IMU 位姿，因子图内部仍使用天线位姿。
- 初始化返回成功状态，失败时不设置完成标志；匹配数据长度不足时不越界读取。
- 按配置订阅 GNSS 话题；离线模式不再积累无消费者的前端 RTK/初始化轨迹队列。
- 改善离线文本输入解析，避免畸形行产生未初始化数据。

GNSS 字段和单位依据 gnss_comm 原始消息定义：
https://github.com/HKUST-Aerial-Robotics/gnss_comm/blob/master/msg/GnssPVTSolnMsg.msg
实时输入要求 valid_fix 且 fix_type 为 3 或 4；不强制 carr_soln 为固定解，保留有效 GNSS 数据并按精度赋权。
旧调试日志只有数值，无法恢复 valid_fix 等状态；缺少精度时使用方差下限。

## 验证与运行

当前 Windows 环境未安装 ROS、GTSAM、Linux 或 C++ 编译器，因此未完成编译、单元测试执行和 rosbag 回放。
已完成原始压缩包差异检查、修改文件括号检查、配置结构检查和输出参考点流程复核。

独立 C++ 边界测试不依赖 ROS，Ubuntu 上可运行：

```bash
g++ -std=c++17 -Wall -Wextra -pedantic -Iinclude tests/rtk_utils_test.cpp -o /tmp/rtk_utils_test
/tmp/rtk_utils_test
```

之后在已安装 README 所列依赖的 catkin 工作空间编译，播放 HH 和 AGV 数据，结束后按 Enter。
重点检查：正常轨迹、禁用 GNSS、完全无 GNSS、短录制、静止/直线运动、GNSS 中断、重复消息、非零天线外参。
静止/共线序列应报告不可观测并安全退出；GNSS 中断超过 max_rtk_gap 不应生成跨中断位置因子。
对非零杆臂数据，比较优化前后 TUM 和 PCD 时均以 IMU 为参考。

仍需实测的限制：GNSS 跳点鲁棒性、样条过冲、粗时间偏移的可信度、长序列内存占用。
本次提供源码修复，不将静态检查等同于已通过运行验证。
